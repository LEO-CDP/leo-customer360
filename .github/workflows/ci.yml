name: CI

on:
  push:
    branches: ['**']
    paths-ignore:
      - 'ui-wireframes/**'
      - 'docs/**'
      - 'frontend-admin/**'
  pull_request:
    branches: [main, master]
    paths-ignore:
      - 'ui-wireframes/**'
      - 'docs/**'
      - 'frontend-admin/**'

jobs:
  unit-tests:
    name: Run all unit tests
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: .

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Python 3.11
        uses: actions/setup-python@v7
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Make test runner executable
        run: chmod +x run_all_tests.sh

      - name: Run all unit tests
        run: |
          ./run_all_tests.sh 2>&1 | tee logs/test-output.log

      - name: Upload test logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: all-unit-test-logs-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            logs/test-output.log
          retention-days: 30

      - name: Send email notification
        if: always()
        uses: dawidd6/action-send-mail@v4
        with:
          server_address: smtp-relay.brevo.com
          server_port: 587
          secure: false
          username: ${{ secrets.BREVO_SMTP_LOGIN }}
          password: ${{ secrets.BREVO_SMTP_KEY }}
          subject: "CI ${{ job.status }}: all unit tests (run #${{ github.run_number }})"
          to: trieu@leocdp.com
          from: ${{ secrets.BREVO_SENDER_EMAIL }}
          body: |
            Workflow: ${{ github.workflow }}
            Job status: ${{ job.status }}
            Repository: ${{ github.repository }}
            Branch/ref: ${{ github.ref }}
            Commit: ${{ github.sha }}
            Triggered by: ${{ github.actor }}
            Run URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          attachments: |
            logs/test-output.log