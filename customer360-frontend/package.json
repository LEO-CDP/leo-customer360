{
  "name": "customer360-frontend-static-js-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Unit tests for static/js/*.js (the browser-side admin SPA). Node's built-in test runner, no build step.",
  "scripts": {
    "test": "node --test static/js/__tests__",
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build"
  },
  "devDependencies": {
    "@storybook/html-vite": "^10.6.0",
    "dompurify": "^3.1.6",
    "jsdom": "^24.1.0",
    "marked": "^12.0.2",
    "playwright": "^1.63.0",
    "storybook": "^10.6.0"
  }
}
