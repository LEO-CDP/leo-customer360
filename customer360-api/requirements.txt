fastapi>=0.111,<1
uvicorn[standard]>=0.30,<1
SQLAlchemy>=2.0,<3
psycopg2-binary>=2.9,<3
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
pydantic[email]
python-dotenv>=1.0,<2
pgvector>=0.3,<1
redis>=5.0,<6

# Dev-mode local JWT issuance/verification (POST /auth/login) -- see
# core/utils/security.py. Already a transitive dep via other packages, but
# pinned explicitly since core/auth.py and core/routers/auth_api.py import
# it directly now.
PyJWT>=2.3,<3

# submit job execution into Dagster server
dagster-graphql

# Test-only dependencies (see ./run_unit_tests.sh)
pytest>=7.4,<9
httpx2>=2.9.1


# OpenTelemetry zero-code request tracing (OTLP -> Jaeger). Installed here; the
# Dockerfile runs `opentelemetry-bootstrap -a install` to pull the matching
# FastAPI/SQLAlchemy/Redis/psycopg2/httpx instrumentors. No app code changes.
# Fully no-op at runtime unless OTEL_SDK_DISABLED=false (see deployments/lib/otel.sh).
opentelemetry-distro>=0.48b0,<1
opentelemetry-exporter-otlp>=1.27,<2
