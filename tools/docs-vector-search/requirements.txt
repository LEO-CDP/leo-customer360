# Hosted-provider RAG stack. Optional local-model packages are in requirements-local.txt.
psycopg[binary]>=3.2      # vDB (PostgreSQL) client
pgvector>=0.3             # pgvector adapter for psycopg
redis>=5.0                 # distributed request limiting
fastapi>=0.110
uvicorn[standard]>=0.29
python-frontmatter>=1.1
python-dotenv>=1.0

# OpenTelemetry zero-code request tracing (OTLP -> Jaeger). `opentelemetry-bootstrap -a install`
# in the Dockerfile installs the FastAPI/psycopg instrumentors; deployments/lib/otel.sh emits the
# OTEL_* env at deploy time. Matches customer360-api / ads-server / frontend-admin.
opentelemetry-distro>=0.48b0,<1
opentelemetry-exporter-otlp>=1.27,<2
