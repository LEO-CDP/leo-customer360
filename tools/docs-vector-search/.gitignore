.venv/
__pycache__/
*.pyc
.env
# Regenerated by `python -m src.enrich`; can be large (vectors). Commit only if you
# want the container to ship a prebuilt index — otherwise build it at deploy time.
data/*.json
