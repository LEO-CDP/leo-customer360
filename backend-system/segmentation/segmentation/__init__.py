"""Segmentation service business logic (backend-system/segmentation).

Standalone package (imported by ../dagster_defs.py), not a dependency of
customer360-api -- this service is deployed independently, same pattern as
backend-system/identity_resolution/identity_resolution/.
"""
